# alchemy-chapter

Install the requirements:
```sh
pip install -r requirements.txt
```

Activate Flask:
```sh
cd app
export FLASK_APP=main.py
flask run
```

Test run:
```sh
cd test
python test.py
```
